import { Routes } from '@angular/router';
import { UserCredentialsComponent } from '../user-credentials/user-credentials.component';

export const routes: Routes = [
  { path: '', component: UserCredentialsComponent }
];
